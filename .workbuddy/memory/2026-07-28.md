# 2026-07-28 工作日志

## 文档同步：README + 子模块 HELP.md
- 项目已从单模块 `log/demo/linkDemo`（IntentClassifier + AgentRouter 一次性派发）重构为 4 模块：
  `summer-common`(零依赖工具包) → `summer-aigc`(Agent 引擎) → `summer-bot`(传输适配器) → `summer-bootstrap`(启动)。
  依赖链单向无环。
- 核心改为双循环 ReAct Agent：`AgentLoop.orchestrate` THINK(chatWithTools+ToolRegistry)→ACT(ToolRegistry.execute)，安全阀 10 轮/120s。
- 11 个 `@Tool`（weather_query/image_generate/image_recognize/image_edit/tts_synthesize/voice_switch/file_analyze/idiom_game/reminder_set/navigation/memory_status），全部返回 ActResult。
- slash 命令仅 `/help` `/status` `/cancel`（SlashInterceptor 预拦截）；其余能力改为自然语言触发 LLM 函数调用。
- 持久化 MyBatis-Plus + Flyway（`summer-aigc/resources/db/migration/V1__initial_schema.sql`）；GlobalExceptionHandler 在 summer-aigc/config（依赖 MessageSender），不在 common。
- 已完成：重写根 `README.md` 反映新架构；在 4 个子模块各新增 `HELP.md`（职责/包结构/关键类/依赖构建/扩展指引）。
- 待办（已知 stale，非本次范围）：`config/ai.yml`、`config/bot.yml` 内注释仍用旧类名，仅注释不影响运行。
